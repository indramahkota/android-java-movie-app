<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.indramahkota.moviecatalogue">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MovieCatalogueApp"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".ui.setting.SettingsActivity"
            android:label="@string/settings" />
        <activity
            android:name=".ui.main.MainActivity"
            android:theme="@style/MainTheme" />
        <activity
            android:name=".ui.detail.MovieDetailsActivity"
            android:label="@string/title_activity_movie_details" />
        <activity
            android:name=".ui.detail.TvShowDetailsActivity"
            android:label="@string/title_activity_tv_show_details" />
        <activity
            android:name=".ui.splash.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:label="@string/search_list_movies"
            android:name=".ui.search.SearchMovieActivity" />
        <activity
            android:label="@string/search_list_tv_shows"
            android:name=".ui.search.SearchTvShowActivity" />
    </application>

</manifest>